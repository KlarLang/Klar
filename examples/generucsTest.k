@Use("java")
public void main() {
    boolean isValid = true;
    // integer a = 2 + isValid;
    // integer a = 2 - isValid;
    // integer a = 2 * isValid;
    // integer a = 2 / isValid;
    //integer a = 2 % isValid;

    println(a);
 
    return null;
}